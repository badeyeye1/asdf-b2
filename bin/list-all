#!/usr/bin/env bash

set -euo pipefail

# shellcheck source=../lib/utils.bash
source "$(dirname "$0")/../lib/utils.bash"

# asdf expects space-separated versions on stdout.
list_all_versions | xargs echo
